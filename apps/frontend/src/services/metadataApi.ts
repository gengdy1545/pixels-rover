import {
  getBackends,
  getSchemas,
  getTables,
  getColumns,
} from './analysisApi';

export { getBackends, getSchemas, getTables, getColumns };

export const metadataApi = {
  getBackends,
  getSchemas,
  getTables,
  getColumns,
};
